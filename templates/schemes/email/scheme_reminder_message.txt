{% if audience == 'CUSTOMER' %}Hello {{ customer_name }},{% else %}Hello,{% endif %}

{% if kind == 'UPCOMING_ELIGIBILITY' %}{% if audience == 'CUSTOMER' %}Your savings scheme {{ scheme_number }} becomes eligible for redemption on {{ eligible_from|date:"d M Y" }}. Reaching this date does not automatically close or redeem your scheme. Please visit our Vellore showroom when you are ready to select jewellery.{% else %}Scheme {{ scheme_number }} for {{ customer_name }} becomes eligible for redemption on {{ eligible_from|date:"d M Y" }} (in {{ eligibility_lead_days }} day{{ eligibility_lead_days|pluralize }}). Eligibility does not automatically close or redeem the account.{% endif %}{% elif kind == 'ALLOCATION_EXCEPTION' %}Contribution {{ contribution_id }} for scheme {{ scheme_number }} is a verified payment awaiting metal allocation. Review the exception and use the existing controlled allocation-retry workflow. This reminder does not alter the contribution or entitlement.{% else %}{% if audience == 'CUSTOMER' %}Redemption {{ redemption_number }} for scheme {{ scheme_number }} was recorded on {{ redemption_completed_at|date:"d M Y" }}. You can review the retained scheme history using the link below.{% else %}Redemption {{ redemption_number }} for {{ customer_name }} under scheme {{ scheme_number }} was recorded on {{ redemption_completed_at|date:"d M Y" }}. Review the immutable redemption ledger using the link below.{% endif %}{% endif %}

Review: {{ action_url }}

This is a transactional service message from Jai Sri Krishna Jewellery. Email-provider acceptance is recorded as delivery evidence, but it does not prove that this message was received or read.
